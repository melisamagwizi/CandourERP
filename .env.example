# Local PostgreSQL connection used by both the app and migrations.
# The app role must NOT be a superuser or have BYPASSRLS, so row-level
# security is actually enforced. See README for the one-time setup.
DATABASE_URL=postgresql://candour_app:devpass@localhost:5432/candour
